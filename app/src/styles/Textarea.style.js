import styled from 'styled-components';

export const Textarea = styled.section`
  font-size: 12px;

  & textarea {
    width: 100%;
    resize: none;
    color: rgb(80, 80, 80);
    font-size: 12px;
    margin-bottom: 8px;
  }
`;
